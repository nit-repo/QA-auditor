# Audit reports written at runtime (on Railway these live on a mounted volume)
reports/

# Build outputs — regenerated by `npm run build`, which `prestart` runs for you
dist/
public/bookmarklet.js

# Dependencies (the project is zero-dependency; this keeps npm noise out)
node_modules/
npm-debug.log*

# Local environment
.env

# OS / editor cruft
.DS_Store
Thumbs.db
*.swp
